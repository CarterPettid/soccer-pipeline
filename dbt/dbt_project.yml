name: 'soccer_pipeline'
version: '1.0.0'

profile: 'soccer_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  soccer_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table